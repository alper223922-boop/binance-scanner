name: Futures Scanner 15m

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Python kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Bagimliliklar
        run: pip install -r requirements.txt

      - name: Scanner 15m calistir
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID_15M }}
          TIMEFRAME: "Min15"
          MIN_VOLUME_USDT: "1000000"
          PYTHONIOENCODING: "utf-8"
        run: python scanner.py
